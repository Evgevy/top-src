const initMenu = () => {
    // const toggleButton = document.querySelector('[data-toggle-menu]');
    // const menu = document.querySelector('.main-nav__menu');

    // toggleButton.addEventListener('click', function() {
    //     const isExpanded = toggleButton.getAttribute('aria-pressed') === 'true';
        
    //     // Переключаем состояние кнопки
    //     toggleButton.setAttribute('aria-pressed', !isExpanded);
        
    //     // Выезжаем или скрываем меню
    //     menu.classList.toggle('is-visible', !isExpanded);
    // });

}

export{initMenu};